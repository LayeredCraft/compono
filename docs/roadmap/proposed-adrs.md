# Proposed ADRs

A status-filtered view of [`docs/adr/README.md`](../adr/README.md): every
ADR that's `Proposed`, or `Accepted` but not yet implemented.

## Current state: no proposed ADRs

Every ADR recorded in [`docs/adr/README.md`](../adr/README.md) is
currently `Accepted`, `Superseded`, or (for the two decisions later
revisions replaced) implicitly retired by their successor — none are
sitting in `Proposed` status, and every `Accepted` ADR has a shipped
implementation. See the [Historical Decision Log](../architecture/decision-log.md)
for the full list.

This page will list any ADR a future design pass proposes but hasn't yet
accepted, so a reader can see what's actively under discussion before it
becomes a committed decision.
