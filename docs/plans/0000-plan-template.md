# [PLAN-NNNN] Title

**Status:** [Not Started | In Progress | Done]

**Implements:** [ADR-NNNN, or a `docs/mvp.md` milestone if this plan spans
more than one ADR — see `docs/plans/README.md`]

## Goal

One or two sentences a reader could use to verify "done" without reading
the rest of the plan.

## Scope

What's in this plan and what's explicitly deferred. Cross-reference the
relevant ADR's Decision Outcome rather than restating it.

## Tasks

Grouped logically (new files, changes to existing files, tests, docs).
Checked off as work proceeds — this section is the part of the plan that
actually changes over its life.

- [ ] Task 1
- [ ] Task 2

## Critical Files

New and modified, so a reviewer (or future you) can see the blast radius
at a glance.

- `path/to/file` — what changes and why

## Test Plan

What gets automated coverage, matching `testing.md`.

## Notes

Anything discovered mid-implementation that changed the plan's shape from
what was originally scoped — a plan being wrong about *how* doesn't
require superseding anything, unlike an ADR being wrong about *what/why*.
