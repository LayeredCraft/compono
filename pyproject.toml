[project]
name = "compono-docs"
version = "0.0.0"
description = "Documentation build dependencies for Compono."
requires-python = ">=3.12"
dependencies = [
    "mkdocs-material>=9.0.0",
    "mkdocs-minify-plugin>=0.7.0",
]
