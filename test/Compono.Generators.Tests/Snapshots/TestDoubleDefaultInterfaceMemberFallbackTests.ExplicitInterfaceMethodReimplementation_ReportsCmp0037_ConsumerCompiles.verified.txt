{
  Diagnostics: [
    {
      Message: 'TestNamespace.ILeaf10' declares member 'Flag', which appears unimplemented from this interface's own declaration but is actually resolved elsewhere in the closure via an explicit interface reimplementation - a shape Compono does not yet recognize during effective-declaration resolution. 'Flag''s unconfigured fallback uses the ordinary computed-default value, which may not match the interface's own resolved behavior.,
      Severity: Info,
      WarningLevel: 1,
      Descriptor: {
        Id: CMP0037,
        Title: Test-double member resolved only by an unrecognized explicit interface reimplementation,
        MessageFormat: '{0}' declares member '{1}', which appears unimplemented from this interface's own declaration but is actually resolved elsewhere in the closure via an explicit interface reimplementation - a shape Compono does not yet recognize during effective-declaration resolution. '{1}''s unconfigured fallback uses the ordinary computed-default value, which may not match the interface's own resolved behavior.,
        Category: Compono.TestDoubles,
        DefaultSeverity: Info,
        IsEnabledByDefault: true
      }
    }
  ]
}