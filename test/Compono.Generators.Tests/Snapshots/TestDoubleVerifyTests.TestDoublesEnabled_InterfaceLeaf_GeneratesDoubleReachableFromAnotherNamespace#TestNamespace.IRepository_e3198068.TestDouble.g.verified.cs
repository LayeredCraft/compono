//HintName: TestNamespace.IRepository_e3198068.TestDouble.g.cs
// <auto-generated/>
#nullable enable

// PLAN-0063/ADR-0060: ClearCalls() reaches every ADR-0049 closed-instantiation-eligible member's
// per-closed-T bucket entries generically, with no static knowledge of which closed T's exist at
// runtime - this non-generic interface (implemented by every generated
// TestNamespace_IRepository_e3198068_*State<T> class below) is what makes that possible without reflection.
internal interface TestNamespace_IRepository_e3198068_IClearableCallState
{
    void ClearObservedCalls();
}

[global::System.CodeDom.Compiler.GeneratedCode("Compono.Generators", "REPLACED")]
internal sealed class TestNamespace_IRepository_e3198068_Double : global::TestNamespace.IRepository
{
    internal delegate global::System.Threading.Tasks.Task<string?> __FindNameAsync_Callback(global::System.Guid id);

    internal readonly ref struct __FindNameAsync_Builder
    {
        private readonly ref global::Compono.ReturnConfig<global::System.Threading.Tasks.Task<string?>> _config;
        private readonly ref __FindNameAsync_Callback? _callback;

        internal __FindNameAsync_Builder(ref global::Compono.ReturnConfig<global::System.Threading.Tasks.Task<string?>> config, ref __FindNameAsync_Callback? callback)
        {
            _config = ref config;
            _callback = ref callback;
        }

        public void Returns(global::System.Threading.Tasks.Task<string?> value)
        {
            _callback = null;
            new global::Compono.ReturnConfigBuilder<global::System.Threading.Tasks.Task<string?>>(ref _config).Returns(value);
        }

        public void Throws(global::System.Exception exception)
        {
            _callback = null;
            new global::Compono.ReturnConfigBuilder<global::System.Threading.Tasks.Task<string?>>(ref _config).Throws(exception);
        }

        public void ReturnsSequence(params global::Compono.SequenceOutcome<global::System.Threading.Tasks.Task<string?>>[] outcomes)
        {
            _callback = null;
            new global::Compono.ReturnConfigBuilder<global::System.Threading.Tasks.Task<string?>>(ref _config).ReturnsSequence(outcomes);
        }

        public void ReturnsCallback(__FindNameAsync_Callback callback)
        {
            global::System.ArgumentNullException.ThrowIfNull(callback);
            _config.ClearConfiguredResponse();
            _callback = callback;
        }
    }
    internal global::Compono.ReturnConfig<int> __Count;
    // ADR-0050: multi-entry response configuration - replaces the single
    // __FindNameAsync/__FindNameAsync_m_{param} shape with an ordered, append-only
    // entry list. Configure() appends; dispatch scans in reverse (last-matching-registration-wins).
    internal sealed class __FindNameAsync_Entry
    {
        internal global::Compono.Match<global::System.Guid>? Matcher_id;
        internal global::Compono.ReturnConfig<global::System.Threading.Tasks.Task<string?>> Config;
        internal __FindNameAsync_Callback? Callback;
    }

    internal readonly global::System.Collections.Generic.List<__FindNameAsync_Entry> __FindNameAsync_entries = [];
    internal readonly global::System.Collections.Generic.List<global::System.Guid> __FindNameAsync_calls = [];
    internal readonly object __FindNameAsync_lock = new();

    // PLAN-0063/ADR-0060: named snapshot record backing ReceivedCalls() for this eligible member -
    // one field per real parameter, real parameter names (not the positional "Item1"/"Item2" the
    // internal __FindNameAsync_calls tuple above uses for its own, unrelated, internal-only
    // matching purpose). Not emitted for an overload-matching-eligible member - ReceivedCalls() is
    // scoped to exactly ADR-0048's non-overloaded eligible-member set for 1.1.
    // A parameter literally named the same as this record's own type (e.g. a real member
    // `Foo(int __Foo_ReceivedCall)`) would otherwise produce a positional property with the same
    // name as its enclosing type - CS0542 - since a record's declared parameter name IS its public
    // property name. member.received_call_record_parameters_text (TestDoubleMemberInfo.ReceivedCallRecordParametersText)
    // renames just that one parameter to a name guaranteed free of every OTHER real parameter's own
    // name too, not merely an unconditional "_Value" suffix - Codex review, PR #134 round 2 caught a
    // real fixture where the unconditional suffix collided with an actual second parameter already
    // named that (`Foo(int __Foo_ReceivedCall, int __Foo_ReceivedCall_Value)`). Rendered fully in C#
    // rather than as an indexed Scriban loop - see the property's own remarks for why.
    internal readonly record struct __FindNameAsync_ReceivedCall(global::System.Guid id);
    // ADR-0050: multi-entry response configuration - replaces the single
    // __Save/__Save_m_{param} shape with an ordered, append-only
    // entry list. Configure() appends; dispatch scans in reverse (last-matching-registration-wins).
    internal sealed class __Save_Entry
    {
        internal global::Compono.Match<string>? Matcher_name;
        internal global::Compono.ReturnConfig<global::Compono.Unit> Config;
    }

    internal readonly global::System.Collections.Generic.List<__Save_Entry> __Save_entries = [];
    internal readonly global::System.Collections.Generic.List<string> __Save_calls = [];
    internal readonly object __Save_lock = new();

    // PLAN-0063/ADR-0060: named snapshot record backing ReceivedCalls() for this eligible member -
    // one field per real parameter, real parameter names (not the positional "Item1"/"Item2" the
    // internal __Save_calls tuple above uses for its own, unrelated, internal-only
    // matching purpose). Not emitted for an overload-matching-eligible member - ReceivedCalls() is
    // scoped to exactly ADR-0048's non-overloaded eligible-member set for 1.1.
    // A parameter literally named the same as this record's own type (e.g. a real member
    // `Foo(int __Foo_ReceivedCall)`) would otherwise produce a positional property with the same
    // name as its enclosing type - CS0542 - since a record's declared parameter name IS its public
    // property name. member.received_call_record_parameters_text (TestDoubleMemberInfo.ReceivedCallRecordParametersText)
    // renames just that one parameter to a name guaranteed free of every OTHER real parameter's own
    // name too, not merely an unconditional "_Value" suffix - Codex review, PR #134 round 2 caught a
    // real fixture where the unconditional suffix collided with an actual second parameter already
    // named that (`Foo(int __Foo_ReceivedCall, int __Foo_ReceivedCall_Value)`). Rendered fully in C#
    // rather than as an indexed Scriban loop - see the property's own remarks for why.
    internal readonly record struct __Save_ReceivedCall(string name);

    global::System.Threading.Tasks.Task<string?> global::TestNamespace.IRepository.FindNameAsync(global::System.Guid id)
    {
        __FindNameAsync_Callback? __callback = null;
        // ADR-0050: reverse-scan the ordered entry list - last matching registration wins. Both
        // the call-log append and the full scan stay under the SAME lock acquisition as
        // Configure()'s Add() (Codex review, PR #108 round 5) - the prior split-lock shape (a
        // short lock around _calls.Add() only, then an unlocked scan) let a concurrent Configure()
        // call mutate List<T>'s backing array while dispatch was still iterating it. `return`/
        // `throw` inside a C# `lock` block still releases the lock (try/finally under the hood).
        lock (__FindNameAsync_lock)
        {
            __FindNameAsync_calls.Add(id);
            for (var __i = __FindNameAsync_entries.Count - 1; __i >= 0; __i--)
            {
                var __entry = __FindNameAsync_entries[__i];
                if ((__entry.Matcher_id is not { } __m_id || __m_id.Matches(id)))
                {
                    if (__entry.Callback is { } configuredCallback)
                    {
                        __callback = configuredCallback;
                        break;
                    }
                    // ADR-0050: no `break` here (Codex review, PR #108 round 6) - if this entry
                    // matched but has neither a configured exception nor a configured value (e.g.
                    // its builder is still being set up when this call arrives), it must NOT shadow
                    // an older, fully-configured matching entry; the scan continues to the next
                    // (older) entry instead of falling through to the default/required-config rule.
                    // ADR-0054: a configured sequence is checked first - Returns/Throws/ReturnsSequence
                    // are mutually exclusive on one Config, so order between this and the two checks
                    // below doesn't change behavior, but leads with the newest-added capability.
                    if (__entry.Config.HasConfiguredSequence) return __entry.Config.NextSequenceOutcome();
                    if (__entry.Config.HasConfiguredException) throw __entry.Config.ConfiguredException;
                    if (__entry.Config.HasConfiguredValue) return __entry.Config.ConfiguredValue;
                }
            }
        }
        if (__callback is { } callback)
            return callback(id);
        return global::System.Threading.Tasks.Task.FromResult<string?>(default);
    }

    void global::TestNamespace.IRepository.Save(string name)
    {
        // ADR-0050 (extended to overloaded members by ADR-0044 Amendment 21 / PLAN-0054 Phase 2):
        // reverse-scan the ordered entry list - last matching registration wins. Both the call-log
        // append and the full scan stay under the SAME lock acquisition as Configure()'s Add()
        // (Codex review, PR #108 round 5) - the prior split-lock shape (a short lock around
        // _calls.Add() only, then an unlocked scan) let a concurrent Configure() call mutate
        // List<T>'s backing array while dispatch was still iterating it.
        lock (__Save_lock)
        {
            __Save_calls.Add(name);
            for (var __i = __Save_entries.Count - 1; __i >= 0; __i--)
            {
                var __entry = __Save_entries[__i];
                if ((__entry.Matcher_name is not { } __m_name || __m_name.Matches(name)))
                {
                    // ADR-0050: no `break` here (Codex review, PR #108 round 6) - see the
                    // value-returning branch below for the full reasoning; a matched entry with
                    // neither a configured exception nor a configured value must not shadow an
                    // older, configured matching entry. Void members still have a genuine
                    // "configured" state distinct from "incomplete" - `HasConfiguredValue` is set
                    // by `.Returns(default)` (a `global::Compono.Unit`) even though there's nothing
                    // to return - so it must
                    // stop the scan (`return;`) exactly like the value-returning branch below, not
                    // be treated as equivalent to an unconfigured/incomplete entry (Codex review,
                    // PR #108 round 7).
                    // ADR-0054: a configured sequence still stops the scan - the sequence's own next
                    // outcome may itself be a configured exception (thrown by NextSequenceOutcome()),
                    // exactly mirroring the exception check immediately below.
                    if (__entry.Config.HasConfiguredSequence) { __entry.Config.NextSequenceOutcome(); return; }
                    if (__entry.Config.HasConfiguredException) throw __entry.Config.ConfiguredException;
                    if (__entry.Config.HasConfiguredValue) return;
                }
            }
        }
    }

    int global::TestNamespace.IRepository.Count
    {
        get
        {
            __Count.RecordCall();
            return __Count.HasConfiguredSequence ? __Count.NextSequenceOutcome()
                : __Count.HasConfiguredException ? throw __Count.ConfiguredException
                : __Count.HasConfiguredValue ? __Count.ConfiguredValue
                : default;
        }
        set
        {
            __Count.RecordCall();
            new global::Compono.ReturnConfigBuilder<int>(ref __Count).Returns(value);
        }
    }
}

internal static class TestNamespace_IRepository_e3198068_DoubleConfiguration
{
    public static global::TestNamespace_IRepository_e3198068_Double.__FindNameAsync_Builder FindNameAsync(this global::TestNamespace_IRepository_e3198068_Double __self, global::Compono.Match<global::System.Guid> id)
    {
        // ADR-0050: appends a new entry - see the closed-instantiation Configure()
        // above for the reallocation-hazard proof, identical reasoning applies here. The Add()
        // itself is under the same member lock dispatch scans under (Codex review, PR #108
        // round 5).
        var __entry = new global::TestNamespace_IRepository_e3198068_Double.__FindNameAsync_Entry();
        __entry.Matcher_id = id;
        lock (__self.__FindNameAsync_lock) { __self.__FindNameAsync_entries.Add(__entry); }
        return new global::TestNamespace_IRepository_e3198068_Double.__FindNameAsync_Builder(ref __entry.Config, ref __entry.Callback);
    }

    // Compatibility overload (Codex review, PLAN-0048): v1/v2 gave every non-overloaded member a
    // zero-argument Configure(), regardless of real arity. ADR-0050: under multi-entry,
    // this no longer needs to null out prior matchers to reproduce "last wins" - it just appends its
    // own new, all-null-matcher (always-matching) entry; being the most-recently-appended entry, the
    // reverse scan finds it before any earlier, more specific entry, exactly reproducing v1/v2's
    // argument-independent override behavior without mutating any earlier entry's state at all.
    public static global::TestNamespace_IRepository_e3198068_Double.__FindNameAsync_Builder FindNameAsync(this global::TestNamespace_IRepository_e3198068_Double self)
    {
        var __entry = new global::TestNamespace_IRepository_e3198068_Double.__FindNameAsync_Entry();
        lock (self.__FindNameAsync_lock) { self.__FindNameAsync_entries.Add(__entry); }
        return new global::TestNamespace_IRepository_e3198068_Double.__FindNameAsync_Builder(ref __entry.Config, ref __entry.Callback);
    }

    public static global::Compono.ReturnConfigBuilder<global::Compono.Unit> Save(this global::TestNamespace_IRepository_e3198068_Double __self, global::Compono.Match<string> name)
    {
        // ADR-0050: appends a new entry - see the closed-instantiation Configure()
        // above for the reallocation-hazard proof, identical reasoning applies here. The Add()
        // itself is under the same member lock dispatch scans under (Codex review, PR #108
        // round 5).
        var __entry = new global::TestNamespace_IRepository_e3198068_Double.__Save_Entry();
        __entry.Matcher_name = name;
        lock (__self.__Save_lock) { __self.__Save_entries.Add(__entry); }
        return new global::Compono.ReturnConfigBuilder<global::Compono.Unit>(ref __entry.Config);
    }

    // Compatibility overload (Codex review, PLAN-0048): v1/v2 gave every non-overloaded member a
    // zero-argument Configure(), regardless of real arity. ADR-0050: under multi-entry,
    // this no longer needs to null out prior matchers to reproduce "last wins" - it just appends its
    // own new, all-null-matcher (always-matching) entry; being the most-recently-appended entry, the
    // reverse scan finds it before any earlier, more specific entry, exactly reproducing v1/v2's
    // argument-independent override behavior without mutating any earlier entry's state at all.
    public static global::Compono.ReturnConfigBuilder<global::Compono.Unit> Save(this global::TestNamespace_IRepository_e3198068_Double self)
    {
        var __entry = new global::TestNamespace_IRepository_e3198068_Double.__Save_Entry();
        lock (self.__Save_lock) { self.__Save_entries.Add(__entry); }
        return new global::Compono.ReturnConfigBuilder<global::Compono.Unit>(ref __entry.Config);
    }

    public static global::Compono.ReturnConfigBuilder<int> Count(this global::TestNamespace_IRepository_e3198068_Double self) =>
        new global::Compono.ReturnConfigBuilder<int>(ref self.__Count);

}

internal readonly struct TestNamespace_IRepository_e3198068_DoubleVerifier
{
    internal global::TestNamespace_IRepository_e3198068_Double Instance { get; }

    internal TestNamespace_IRepository_e3198068_DoubleVerifier(global::TestNamespace_IRepository_e3198068_Double instance) => Instance = instance;
}

internal static class TestNamespace_IRepository_e3198068_VerifyExtension
{
    public static global::TestNamespace_IRepository_e3198068_DoubleVerifier Verify(this global::TestNamespace.IRepository self) =>
        new(self as global::TestNamespace_IRepository_e3198068_Double
            ?? throw new global::System.InvalidOperationException(
                $"'{self.GetType()}' is not the 'global::TestNamespace.IRepository' test double generated for this assembly. " +
                "If another assembly in this process also generated a double for 'global::TestNamespace.IRepository', only one " +
                "registration wins process-wide (Compono.GeneratedTestDoubleRegistry, first-registration-wins) " +
                "- this is a known v1 limitation, not a bug in your test."));
}

internal static class TestNamespace_IRepository_e3198068_DoubleVerification
{
    public static global::Compono.CallVerifier FindNameAsync(this global::TestNamespace_IRepository_e3198068_DoubleVerifier __self, global::Compono.Match<global::System.Guid> id)
    {
        int __count;
        lock (__self.Instance.__FindNameAsync_lock)
        {
            __count = 0;
            foreach (var call in __self.Instance.__FindNameAsync_calls)
            {
                if (id.Matches(call))
                    __count++;
            }
        }
        return new(__count, "global::TestNamespace.IRepository.FindNameAsync");
    }

    // Compatibility overload - ADR-0050: the removed single-slot field no longer
    // tracks a call count at all (RecordCall() is gone from dispatch for this shape) - the call
    // log's own Count, under its existing lock, is exactly the same number and is already
    // maintained regardless of how many response entries exist.
    public static global::Compono.CallVerifier FindNameAsync(this global::TestNamespace_IRepository_e3198068_DoubleVerifier self)
    {
        int __count;
        lock (self.Instance.__FindNameAsync_lock) { __count = self.Instance.__FindNameAsync_calls.Count; }
        return new(__count, "global::TestNamespace.IRepository.FindNameAsync");
    }

    public static global::Compono.CallVerifier Save(this global::TestNamespace_IRepository_e3198068_DoubleVerifier __self, global::Compono.Match<string> name)
    {
        int __count;
        lock (__self.Instance.__Save_lock)
        {
            __count = 0;
            foreach (var call in __self.Instance.__Save_calls)
            {
                if (name.Matches(call))
                    __count++;
            }
        }
        return new(__count, "global::TestNamespace.IRepository.Save");
    }

    // Compatibility overload - ADR-0050: the removed single-slot field no longer
    // tracks a call count at all (RecordCall() is gone from dispatch for this shape) - the call
    // log's own Count, under its existing lock, is exactly the same number and is already
    // maintained regardless of how many response entries exist.
    public static global::Compono.CallVerifier Save(this global::TestNamespace_IRepository_e3198068_DoubleVerifier self)
    {
        int __count;
        lock (self.Instance.__Save_lock) { __count = self.Instance.__Save_calls.Count; }
        return new(__count, "global::TestNamespace.IRepository.Save");
    }

    public static global::Compono.CallVerifier Count(this global::TestNamespace_IRepository_e3198068_DoubleVerifier self) =>
        new(self.Instance.__Count.ConfiguredCallCount, "global::TestNamespace.IRepository.Count");

}

// PLAN-0063/ADR-0060: ReceivedCalls() - retrospective, snapshot-based call inspection, scoped to
// exactly the ADR-0048 eligible-member set (member.is_eligible_for_matching), unexpanded. A
// distinct third bridge from Configure()/Verify() - never attaches captured data to CallVerifier.
internal readonly struct TestNamespace_IRepository_e3198068_DoubleReceivedCalls
{
    internal global::TestNamespace_IRepository_e3198068_Double Instance { get; }

    internal TestNamespace_IRepository_e3198068_DoubleReceivedCalls(global::TestNamespace_IRepository_e3198068_Double instance) => Instance = instance;
}

internal static class TestNamespace_IRepository_e3198068_ReceivedCallsExtension
{
    public static global::TestNamespace_IRepository_e3198068_DoubleReceivedCalls ReceivedCalls(this global::TestNamespace.IRepository self) =>
        new(self as global::TestNamespace_IRepository_e3198068_Double
            ?? throw new global::System.InvalidOperationException(
                $"'{self.GetType()}' is not the 'global::TestNamespace.IRepository' test double generated for this assembly. " +
                "If another assembly in this process also generated a double for 'global::TestNamespace.IRepository', only one " +
                "registration wins process-wide (Compono.GeneratedTestDoubleRegistry, first-registration-wins) " +
                "- this is a known v1 limitation, not a bug in your test."));
}

internal static class TestNamespace_IRepository_e3198068_DoubleReceivedCallsAccess
{
    // Snapshot under the same ADR-0048 per-member lock Verify()'s own scan already uses - no live
    // mutable collection is ever returned, per ADR-0060's snapshot-semantics decision.
    public static global::System.Collections.Generic.IReadOnlyList<global::TestNamespace_IRepository_e3198068_Double.__FindNameAsync_ReceivedCall> FindNameAsync(this global::TestNamespace_IRepository_e3198068_DoubleReceivedCalls self)
    {
        lock (self.Instance.__FindNameAsync_lock)
        {
            var __snapshot = new global::TestNamespace_IRepository_e3198068_Double.__FindNameAsync_ReceivedCall[self.Instance.__FindNameAsync_calls.Count];
            for (var __i = 0; __i < __snapshot.Length; __i++)
            {
                var call = self.Instance.__FindNameAsync_calls[__i];
                __snapshot[__i] = new(call);
            }

            return __snapshot;
        }
    }

    // Snapshot under the same ADR-0048 per-member lock Verify()'s own scan already uses - no live
    // mutable collection is ever returned, per ADR-0060's snapshot-semantics decision.
    public static global::System.Collections.Generic.IReadOnlyList<global::TestNamespace_IRepository_e3198068_Double.__Save_ReceivedCall> Save(this global::TestNamespace_IRepository_e3198068_DoubleReceivedCalls self)
    {
        lock (self.Instance.__Save_lock)
        {
            var __snapshot = new global::TestNamespace_IRepository_e3198068_Double.__Save_ReceivedCall[self.Instance.__Save_calls.Count];
            for (var __i = 0; __i < __snapshot.Length; __i++)
            {
                var call = self.Instance.__Save_calls[__i];
                __snapshot[__i] = new(call);
            }

            return __snapshot;
        }
    }

}

// PLAN-0063/ADR-0060: ClearCalls() - a direct, whole-double operation clearing every member's
// observation state (scalar call counts + ADR-0048 captured argument histories) while preserving
// all configured behavior (Returns/Throws/ReturnsCallback/ReturnsSequence - including in-progress
// SequenceOrdinal, which does not rewind - argument-matcher/multi-entry configuration, and generic
// closed-instantiation configuration). No per-member ClearCalls(), no global whole-double lock -
// each member's own existing synchronization primitive is used independently, per member.
internal static class TestNamespace_IRepository_e3198068_ClearCallsExtension
{
    public static void ClearCalls(this global::TestNamespace.IRepository self)
    {
        var __double = self as global::TestNamespace_IRepository_e3198068_Double
            ?? throw new global::System.InvalidOperationException(
                $"'{self.GetType()}' is not the 'global::TestNamespace.IRepository' test double generated for this assembly. " +
                "If another assembly in this process also generated a double for 'global::TestNamespace.IRepository', only one " +
                "registration wins process-wide (Compono.GeneratedTestDoubleRegistry, first-registration-wins) " +
                "- this is a known v1 limitation, not a bug in your test.");

        lock (__double.__FindNameAsync_lock) { __double.__FindNameAsync_calls.Clear(); }
        lock (__double.__Save_lock) { __double.__Save_calls.Clear(); }
        __double.__Count.ClearObservedCalls();
    }
}

internal static class TestNamespace_IRepository_e3198068_ConfigureExtension
{
    public static global::TestNamespace_IRepository_e3198068_Double Configure(this global::TestNamespace.IRepository self) =>
        self as global::TestNamespace_IRepository_e3198068_Double
            ?? throw new global::System.InvalidOperationException(
                $"'{self.GetType()}' is not the 'global::TestNamespace.IRepository' test double generated for this assembly. " +
                "If another assembly in this process also generated a double for 'global::TestNamespace.IRepository', only one " +
                "registration wins process-wide (Compono.GeneratedTestDoubleRegistry, first-registration-wins) " +
                "- this is a known v1 limitation, not a bug in your test.");
}

file static class TestNamespace_IRepository_e3198068_DoubleRegistration
{
    [global::System.Runtime.CompilerServices.ModuleInitializer]
    internal static void Register() =>
        global::Compono.GeneratedTestDoubleRegistry.RegisterFactory<global::TestNamespace.IRepository>(
            () => new global::TestNamespace_IRepository_e3198068_Double());
}
