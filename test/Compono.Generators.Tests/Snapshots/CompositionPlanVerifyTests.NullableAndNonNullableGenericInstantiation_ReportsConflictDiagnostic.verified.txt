{
  Diagnostics: [
    {
      Message: 'global::TestNamespace.Box<string>' was discovered multiple times with different composition metadata (for example, Create<Box<string>>() and Create<Box<string?>>() in the same compilation) - Compono generates exactly one plan per type and can't guarantee it correctly reflects every discovery. Request this type with consistent nullability everywhere it's composed.,
      Severity: Error,
      Descriptor: {
        Id: CMP0010,
        Title: Conflicting composition metadata across discoveries,
        MessageFormat: '{0}' was discovered multiple times with different composition metadata (for example, Create<Box<string>>() and Create<Box<string?>>() in the same compilation) - Compono generates exactly one plan per type and can't guarantee it correctly reflects every discovery. Request this type with consistent nullability everywhere it's composed.,
        Category: Compono.Usage,
        DefaultSeverity: Error,
        IsEnabledByDefault: true
      }
    }
  ]
}