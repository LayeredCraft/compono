<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <packageSources>
    <clear />
    <!-- Populated by this project's own PackToLocalFeed pre-restore target (see the .csproj) -
         never committed, regenerated from current source on every restore. -->
    <add key="compono-local" value="../../.local-nuget-feed-nunit-compat-matrix" />
    <add key="nuget.org" value="https://api.nuget.org/v3/index.json" />
  </packageSources>
</configuration>
