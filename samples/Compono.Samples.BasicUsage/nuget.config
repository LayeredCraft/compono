<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <packageSources>
    <clear />
    <!-- Only consulted in packed-verification mode (dotnet test -p:UsePackedPackages=true) - the
         default dev build uses ProjectReference and never needs this source. Populated by this
         project's own PackToLocalFeed pre-restore target; never committed. -->
    <add key="compono-local" value="../../.local-nuget-feed" />
    <add key="nuget.org" value="https://api.nuget.org/v3/index.json" />
  </packageSources>
</configuration>
